--- src/components/Home/HomeBlog.jsx
+++ src/components/Home/HomeBlog.jsx
@@ -1,5 +1,5 @@
-import React, { useEffect, useRef } from 'react';
-import { Link, useNavigate } from 'react-router-dom';
+import React from 'react';
+import { Link } from 'react-router-dom';
 import { ROUTES } from '../../constants/routes';
 import { useTranslation } from '../../i18n/LanguageContext';
 
@@ -8,33 +8,6 @@
-  const carouselRef = useRef(null);
 
   // Extract unique categories from posts, ignore missing
   const categories = [...new Set(posts.map(post => post.category).filter(Boolean))];
-
-  const navigate = useNavigate();
-
-  // Intercept clicks on links inside the jQuery carousel 
-  // because cloned nodes lose React Router event listeners (causes full page reloads)
-  useEffect(() => {
-    const el = carouselRef.current;
-    if (!el) return;
-
-    const handleCarouselClick = (e) => {
-      const aClick = e.target.closest('a');
-      if (aClick && aClick.getAttribute('href')) {
-        const href = aClick.getAttribute('href');
-        if (href.startsWith('#')) return; // Ignore hash links
-        
-        const baseUrl = import.meta.env.BASE_URL || '/';
-        if (href.startsWith(baseUrl)) {
-          e.preventDefault();
-          const targetPath = href.substring(baseUrl.length - 1); // Keeping leading slash
-          navigate(targetPath);
-        }
-      }
-    };
-
-    el.addEventListener('click', handleCarouselClick);
-    return () => el.removeEventListener('click', handleCarouselClick);
-  }, [navigate]);
 
 	return (
@@ -52,14 +25,14 @@
 						<div className="row justify-content-center">
 							<div className="col-md-10 col-xl-6">
 								<div className="filters gallery-filters small-text text-lg-right">
-									<a href="#all" onClick={(e) => e.preventDefault()} data-filter="*" className="active selected">{t('home.blog.allCategories')}</a>
+									<a href="#" data-filter="*" className="active selected">{t('home.blog.allCategories')}</a>
 									{categories.map(cat => {
 										const filterClass = cat.toLowerCase().replace(/\s+/g, '-');
 										return (
-											<a key={filterClass} href={`#${filterClass}`} onClick={(e) => e.preventDefault()} data-filter={`.${filterClass}`}>{cat}</a>
+											<a key={filterClass} href="#" data-filter={`.${filterClass}`}>{cat}</a>
 										);
 									})}
 								</div>
 							</div>
 						</div>
-						<div ref={carouselRef} className="owl-carousel  carousel-nav" data-responsive-lg="3" data-responsive-md="2" data-responsive-sm="2" data-responsive-xs="1" data-nav="true" data-loop="true" data-filters=".gallery-filters">
+						<div className="owl-carousel  carousel-nav" data-responsive-lg="3" data-responsive-md="2" data-responsive-sm="2" data-responsive-xs="1" data-nav="true" data-loop="true" data-filters=".gallery-filters">
               {posts.map((post) => {
