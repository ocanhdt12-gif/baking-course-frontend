# Vite Environment Variables (Frontend)
# Copy this file to .env or .env.production depending on your environment

# The Base URL where the Node.js API is running.
VITE_API_BASE_URL=http://localhost:5001/api

# For production deployment with NGINX reverse proxy, use:
# VITE_API_BASE_URL=/api
