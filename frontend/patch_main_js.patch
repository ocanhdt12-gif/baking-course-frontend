--- public/js/main.js
+++ public/js/main.js
@@ -1029,9 +1029,14 @@
 				filters = data.filters ? data.filters : false;
 
 			if (filters) {
-				$carousel.after($carousel.clone().addClass('owl-carousel-filter-cloned'));
+                // FIX: Only clone if not already cloned (Strict Mode safeguard)
+                if (!$carousel.next().hasClass('owl-carousel-filter-cloned')) {
+				    $carousel.after($carousel.clone().addClass('owl-carousel-filter-cloned'));
+                }
+                // Re-bind click defensively
+                $(filters).off('click', 'a').on('click', 'a', function( e ) {
-				$(filters).on('click', 'a', function( e ) {
 					//processing filter link
 					e.preventDefault();
 					if ($(this).hasClass('selected')) {
@@ -1043,9 +1048,9 @@
 					
 					//removing old items
 					for (var i = $carousel.find('.owl-item').length - 1; i >= 0; i--) {
-						$carousel.trigger('remove.owl.carousel', [1]);
+						$carousel.trigger('remove.owl.carousel', [i]);
 					};
 
 					//adding new items
